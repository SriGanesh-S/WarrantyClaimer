<div class="col-lg-10" style="background-color: rgba(0, 0, 0, 0.5); padding: 20px; border-radius: 10px; margin-top: 50px;">
  <h1 style="color: #fff; text-align: center;">Edit Claim Resolution</h1>
<div class="col-lg-10" >
<%= form_with(model: @claim_resolution) do |form| %>
  <% if @claim_resolution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(claim_resolution.errors.count, "error") %> prohibited this claim_resolution from being saved:</h2>

      <ul>
        <% @claim_resolution.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
<div class="form-group">
    <strong> <%= form.label :status %> </strong> 
     <%= form.select :status,[['In Progress', 'In Progress'], ['Accepeted', 'Accepted'], ['Rejected', 'Rejected'], ['Shipped', 'Shipped'], ['Closed', 'Closed']],class:"form-control" %>
</div>
<div class="form-group">
   <strong>   <%= form.label :Description %></strong> 
     <%= form.text_field :description ,class:"form-control"%>
</div>


  <div class="actions">
    <%= form.submit %>
    
  </div>
<% end %>
</div>


</div>